import TextEditor from "../../components/Blogs/TextEditor";

const CreateBlogs: React.FC = (): React.JSX.Element => {
  return (
    <div className="h-dvh w-full bg-[#292a2b]">
      <TextEditor />
    </div>
  );
};

export default CreateBlogs;
